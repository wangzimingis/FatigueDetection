# 核心深度学习框架
torch>=2.0.1
torchvision>=0.15.2

# 数值计算与数据处理
numpy>=1.21.0
scipy>=1.9.0
pandas>=1.5.0
scikit-learn>=1.2.0

# 机器学习与信号处理
pywavelets>=1.4.0
einops>=0.6.0

# 数据可视化
matplotlib>=3.5.0
seaborn>=0.12.0
tensorboard>=2.11.0

# 训练进度与I/O
tqdm>=4.64.0
h5py>=3.7.0

# Excel 写入支持（用于保存训练历史）
openpyxl>=3.0.0

# 系统资源监控
psutil>=5.9.0

# 可选：降维可视化（t-SNE 已内置在 sklearn，UMAP 需要额外安装）
umap-learn>=0.5.5

# 可选：GPU 信息获取（如果使用 GPUtil）
GPUtil>=1.4.0

# 可选：模型结构图（torchviz 依赖 Graphviz，需单独安装软件）
torchviz>=0.0.2
graphviz>=0.20.0

# Jupyter 交互环境（可选）
jupyter>=1.0.0
ipywidgets>=8.0.0